[info][title]チケット #<%= event.issue.id %> が <%= event.issue.author.lastname %> <%= event.issue.author.firstname %> によって更新されました。[/title]<% event.journal.details.forEach(function(detail){ -%>
<% if (detail.prop_key == 'description') { -%>
説明 を更新
<% } else { -%>
<%= detail.prop_key %> を <%= detail.old_value %> から <%= detail.value %> に変更
<% } -%>
<% }); -%>
<% if (event.journal.notes != "") { %>[code]<%- event.journal.notes %>[/code]<% } %>[hr]<%= event.issue.project.name %> <%= event.issue.tracker.name %> #<%= event.issue.id %>:<%= event.issue.subject %>
<%= event.url %>
* 作成者: <%= event.issue.author.lastname %> <%= event.issue.author.firstname %>
* ステータス: <%= event.issue.status.name %>
* 優先度: <%= event.issue.priority.name %>
<% if (event.issue.assignee) { -%>* 担当者: <%= event.issue.assignee.lastname %> <%= event.issue.assignee.firstname %><% } -%>[/info]
